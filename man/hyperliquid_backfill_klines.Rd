% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{hyperliquid_backfill_klines}
\alias{hyperliquid_backfill_klines}
\title{Backfill Hyperliquid Candle (OHLCV) Data to CSV}
\usage{
hyperliquid_backfill_klines(
  symbols,
  intervals = "1d",
  from = lubridate::now("UTC") - lubridate::ddays(365),
  to = lubridate::now("UTC"),
  file = "hyperliquid_klines.csv",
  testnet = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{symbols}{(character) canonical coin symbols (e.g. \code{c("BTC", "ETH")}).
Must not be NULL or empty.}

\item{intervals}{(character in HYPERLIQUID_INTERVALS) candle intervals (e.g.
\code{c("1d", "1h")}). Each must be one of \link{HYPERLIQUID_INTERVALS}. Default
\code{"1d"}.}

\item{from}{(POSIXct | numeric) start of the backfill window. Defaults to one
year ago.}

\item{to}{(POSIXct | numeric) end of the window. Defaults to the current
time.}

\item{file}{(scalar<character>) path to the output CSV. Data is appended
incrementally.}

\item{testnet}{(scalar<logical>) target testnet instead of mainnet. Default
\code{FALSE}.}

\item{sleep}{(scalar<numeric in [0, Inf[>) seconds to sleep between each
\verb{(symbol, interval)} combination to respect rate limits. Default \code{0.3}.}

\item{verbose}{(scalar<logical>) if \code{TRUE}, prints progress via
\code{\link[rlang:abort]{rlang::inform()}}.}
}
\value{
(scalar<character>) the file path (invisibly). Output columns:
\code{symbol}, \code{interval}, \code{datetime}, \code{open}, \code{high}, \code{low}, \code{close}, \code{volume},
\code{trades}.

Per-combo failures are surfaced as warnings during the run (one
\code{\link[rlang:abort]{rlang::warn()}} per failed \verb{(symbol, interval)} pair), followed by a final
summary warning if any failed. No failure data is hidden on the return
value.
}
\description{
Downloads historical OHLCV candles for one or more coins and intervals,
writing results incrementally to a CSV file. Supports resuming a partially
completed backfill by reading the existing file and continuing each
\verb{(symbol, interval)} series from its last stored candle.
}
\details{
Hyperliquid's \code{candleSnapshot} returns only the ~5000 most recent candles per
interval, so deep intraday history is not available over REST; coarse
intervals (\code{"1d"}, \code{"1h"}) reach back furthest. Within that cap
\code{hyperliquid_fetch_klines()} segments the range automatically.
}
\examples{
\dontrun{
hyperliquid_backfill_klines(
  symbols = c("BTC", "ETH"),
  intervals = c("1d", "1h"),
  from = lubridate::as_datetime("2024-01-01"),
  file = "hyperliquid_klines.csv"
)
}
}
