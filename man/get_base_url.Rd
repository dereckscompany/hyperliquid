% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_base_url}
\alias{get_base_url}
\title{Retrieve the Hyperliquid REST Base URL}
\usage{
get_base_url(testnet = FALSE)
}
\arguments{
\item{testnet}{Logical; if \code{TRUE} return the testnet host, otherwise the
mainnet host. Default \code{FALSE}.}
}
\value{
Character string; the REST base URL.
}
\description{
Returns the REST base URL for the selected network. Hyperliquid exposes the
entire API behind one host (two paths, \verb{/info} and \verb{/exchange}); only the
network differs, and it must be chosen by an explicit flag because the
network changes the \emph{signature itself} (phantom-agent source and
\code{hyperliquidChain} tag), so URL sniffing is never safe.
}
\examples{
get_base_url()
get_base_url(testnet = TRUE)

}
