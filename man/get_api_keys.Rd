% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{get_api_keys}
\alias{get_api_keys}
\title{Retrieve Hyperliquid Wallet Credentials}
\usage{
get_api_keys(
  private_key = Sys.getenv("HYPERLIQUID_PRIVATE_KEY"),
  account_address = Sys.getenv("HYPERLIQUID_ACCOUNT_ADDRESS")
)
}
\arguments{
\item{private_key}{(scalar<character>) the signing key. Defaults to
\code{Sys.getenv("HYPERLIQUID_PRIVATE_KEY")}.}

\item{account_address}{(scalar<character>) the optional master account
address for an agent wallet. Defaults to
\code{Sys.getenv("HYPERLIQUID_ACCOUNT_ADDRESS")}.}
}
\value{
(list) named list with:
\itemize{
\item private_key (vector<raw, 32> | NULL) signing scalar, or \code{NULL} when absent.
\item account_address (scalar<character> | NULL) master address, or \code{NULL}.
\item wallet_address (scalar<character> | NULL) \verb{0x}-prefixed address derived from
the key, or \code{NULL} when absent.
}
}
\description{
Reads the signing wallet's private key from the environment (or explicit
arguments) and derives the wallet address from it. Hyperliquid uses no API
keys: requests to \verb{/exchange} are authenticated by an Ethereum wallet
signature in the body.
}
\details{
Required environment variable: \code{HYPERLIQUID_PRIVATE_KEY} (a 64-hex-character
secp256k1 key). Optional \code{HYPERLIQUID_ACCOUNT_ADDRESS} names the master
account when the key is an \strong{agent/API wallet} acting on its behalf.

When no key is present this \strong{warns} (it does not abort): public \verb{/info}
market data works without credentials, so a key-less client is still useful.

\strong{Read-only mode}: when \code{account_address} is set but the private key is
absent, the address is KEPT (not discarded) — every account-state \verb{/info}
read (positions, balances, margin summary) works against that address, while
signing \verb{/exchange} actions stays impossible. This is deliberate: a forward
test or monitoring process can watch a real account with credentials that
are physically incapable of placing an order.
}
\examples{
\dontrun{
keys <- get_api_keys()
}

}
