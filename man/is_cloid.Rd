% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloid.R
\name{is_cloid}
\alias{is_cloid}
\title{Test Whether a Value Is a Valid cloid}
\usage{
is_cloid(x)
}
\arguments{
\item{x}{(character | NA) the values to test.}
}
\value{
(logical | NA) one flag per element: \code{TRUE} when the element is a
valid cloid, \code{FALSE} otherwise, and \code{NA} where \code{x} is \code{NA}.
}
\description{
Vectorised, non-aborting predicate: \code{TRUE} for each element that matches
Hyperliquid's client-order-id format (a \verb{0x} prefix followed by exactly 32 hex
characters, i.e. 16 bytes), matched case-insensitively. \code{NA} in maps to \code{NA}
out. Unlike the internal order-boundary validator this never aborts, so it is
safe to use as a filter or a boundary guard. For the canonical (lowercased)
form, and to derive a cloid from an arbitrary tag, see \code{\link[=as_cloid]{as_cloid()}}.
}
\examples{
is_cloid("0x1234567890abcdef1234567890abcdef")
is_cloid(c("0xNOTHEX", NA, new_cloid()))

}
