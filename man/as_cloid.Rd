% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cloid.R
\name{as_cloid}
\alias{as_cloid}
\title{Coerce Any Client Tag to a Canonical cloid}
\usage{
as_cloid(x)
}
\arguments{
\item{x}{(character | NA) the client tags to coerce.}
}
\value{
(character | NA) one canonical cloid per element, and \code{NA} where \code{x}
is \code{NA}. Idempotent: \code{as_cloid(as_cloid(x))} equals \code{as_cloid(x)}.
}
\description{
Deterministically maps any character tag onto a venue-valid cloid, so a
free-form idempotency id can be used on Hyperliquid, which accepts only the
fixed \verb{0x} + 32-hex (16-byte) form. Each element is mapped as follows:
}
\details{
\itemize{
\item a value that is already a valid cloid (any case) is returned lowercased and
otherwise unchanged (the canonical form);
\item any other tag becomes \code{"0x"} followed by the first 16 bytes of
\code{openssl::sha256(charToRaw(x))} rendered as 32 lowercase hex characters;
\item \code{NA} maps to \code{NA}.
}

The mapping is deterministic: the same tag always yields the same cloid, which
is exactly what makes it usable as an idempotency key, and the 128-bit width
makes collisions negligible.

The hash step is \strong{one-way}: a tag that is not already a cloid cannot be
recovered from its cloid. Recover the original by \strong{lookup} on your side
(match the venue-returned cloid against \code{as_cloid()} of your candidate ids, or
keep a local tag -> cloid map), never by decoding. There is deliberately no
\code{cloid_decode()}.
}
\examples{
as_cloid("my-run-42:BTC:entry")
as_cloid("0xABCDEF7890ABCDEF1234567890ABCDEF")

}
