% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/backfill.R
\name{hyperliquid_backfill_funding}
\alias{hyperliquid_backfill_funding}
\title{Backfill Hyperliquid Funding-Rate History to CSV}
\usage{
hyperliquid_backfill_funding(
  symbols,
  from = lubridate::now("UTC") - lubridate::ddays(365),
  to = lubridate::now("UTC"),
  file = "hyperliquid_funding.csv",
  testnet = FALSE,
  sleep = 0.3,
  verbose = TRUE
)
}
\arguments{
\item{symbols}{Character vector of canonical coin symbols (e.g.
\code{c("BTC", "ETH")}). Must not be NULL or empty.}

\item{from}{POSIXct or numeric; start of the backfill window. Defaults to one
year ago.}

\item{to}{POSIXct or numeric; end of the window. Defaults to the current time.}

\item{file}{Character; path to the output CSV. Data is appended incrementally.}

\item{testnet}{Logical; target testnet instead of mainnet. Default \code{FALSE}.}

\item{sleep}{Numeric; seconds to sleep between pages and between coins to
respect rate limits. Default \code{0.3}.}

\item{verbose}{Logical; if \code{TRUE}, prints progress via \code{\link[rlang:abort]{rlang::inform()}}.}
}
\value{
The file path (invisibly). Output columns: \code{coin}, \code{funding_rate},
\code{premium}, \code{time}.

Per-coin failures are surfaced as warnings during the run, followed by a
final summary warning if any failed. No failure data is hidden on the
return value.
}
\description{
Downloads the funding-rate history for one or more coins, writing results
incrementally to a CSV file. Unlike candles, Hyperliquid's \code{fundingHistory} is
complete and free, so this walks the full window in \code{page_limit}-sized time
steps. Supports resuming by reading the existing file and continuing each coin
from its last stored funding record.
}
\examples{
\dontrun{
hyperliquid_backfill_funding(
  symbols = c("BTC", "ETH"),
  from = lubridate::as_datetime("2024-01-01"),
  file = "hyperliquid_funding.csv"
)
}
}
